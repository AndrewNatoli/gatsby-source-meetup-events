{
  "name": "gatsby-source-meetup-events",
  "version": "0.0.1",
  "description": "Pulls events for a group from MeetUp's public API for use in Gatsby",
  "main": "gatsby-node.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "watch": "tsc -w"
  },
  "author": "Andrew Natoli",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^3.6.2"
  },
  "dependencies": {
    "@types/node-fetch": "^2.5.0",
    "node-fetch": "^2.6.7"
  }
}
